# Fresh placed actor and firing-source provenance

`src/combat-initial-runtime.ts` is original WebRA2 code, copyright 2026 WebRA2
contributors, licensed GPL-3.0-or-later. The original fixtures in
`tests/content/combat-initial-fixture.ts` and `combat-initial-runtime.test.ts` use
the same license. No executable code, decompiler output, retail INI rows or art
is included. Dependencies are the already attributed WebRA2 construction,
source-view, actor/entity and numeric components; no new dependency is adopted.

The factual specification comes from bounded static inspection of the local Steam
Traditional Chinese installation. Image, selected range and interpretation pins
are in [the component report](../../docs/combat-initial-runtime.md). Native code
was read, never executed. This is not a formal clean-room claim. A source timing
program establishes neither current live eligibility nor full firing compatibility.
