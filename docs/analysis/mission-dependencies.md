# Remaining opening-mission dependency closure

The [candidate graph](campaign-graph.md) is incomplete by design. These concrete
edges remain under [effective-content issue #18](https://github.com/lictl/WebRA2/issues/18)
and the M0 gate. No owner observation is required to finish the current graph slice.

| Unresolved edge or behavior | Evidence available | Next bounded action |
| --- | --- | --- |
| Definition candidate → effective RA2/YR layer | Two rulesmd hashes, duplicate sections and map overrides survive | Consume independently reviewed profile/precedence policy; preserve alternative fingerprints |
| Country/owner selector → runtime house | Literal country fields and editor selector code; sentinel occurrences retained | Specify selectors independently of concrete houses; compare one original mission ownership case if static evidence cannot settle it |
| Object → primary/secondary/elite weapon | Present schema field occurrence counts, with no values published | Resolve rule section references after precedence; document numeric/index weapon variants and inherited defaults |
| Weapon → projectile / warhead | Listed as an unsupported capability; no weapon sections compiled yet | Extract bounded literal references from effective rule definitions with pinned primary format evidence |
| Object → prerequisite / deploy / undeploy / upgrade | Per-field untraced diagnostics for encountered type candidates | Define token alternatives and built-in groups before treating them as object references |
| Object → voices / explosion / debris animation | Per-field untraced counts | Resolve sound and animation tables, retaining lists and fallback policies; then census reachable file formats |
| Type → implicit image / theater-specific art → SHP/VXL/HVA/TMP/palette | Explicit Image links only; no implicit fallback or actual file resolution | Implement content lookup against an effective VFS, including implicit type-name art and theater variants |
| Packed cells / overlay / terrain / building upgrades → type and visual assets | Placement prefix only; packed formats not decoded | Add bounded map-pack/type extraction; avoid treating visible placement nodes as the entire scene |
| Trigger/action/event/script opcode → referenced object, team, waypoint, variable, media or scheduled work | Opening opcode IDs from prior framing census | Adopt typed, evidence-backed opcode parameter schemas and count unsupported operands; no guessed opcode meanings |
| AI trigger condition / script / recruitment → active team and object | Literal owner/team/type references retained | Specify activation and selector semantics; do not treat all static candidates as active requirements |
| Cinematic identifier → localized Bink/VQA/audio/subtitle resources | Basic cinematic field presence and source locations | Resolve media-name tables/profile variants and measure actual decode/presentation path |
| Battle/mission table entry → next native mission and win/lose transition | Membership and filename/hash references; legacy Basic fields explicitly ignored | Find runtime-consumed progression evidence; request one focused owner observation only if needed |

The graph's `structuralClosureComplete`, `behaviorClosureComplete` and the report's
`dependencyClosureComplete` remain false. Zero missing literal references is not
enough: unsupported transitive edges can hide further assets or mechanics. Counts
across all indexed rules/AI/art must not be confused with the separately reported
reachable subset. These are planning inputs for the first playable slices, not
claims that either opening campaign can run.
